MCQ-Database
============

Simple multiple choice question database with a web interface.

It uses JSP to retrieve information from the database.

And, to add new ojects to the database it uses AJAX to consume a REST web service implemented using Java Spring.

